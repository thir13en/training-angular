import { ResizeObserverDirectiveDirective } from './resize-observer-directive.directive';

describe('ResizeObserverDirectiveDirective', () => {
  it('should create an instance', () => {
    const directive = new ResizeObserverDirectiveDirective();
    expect(directive).toBeTruthy();
  });
});
