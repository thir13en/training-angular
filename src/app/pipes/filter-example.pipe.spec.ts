import { FilterExamplePipe } from './filter-example.pipe';

describe('FilterExamplePipe', () => {
  it('create an instance', () => {
    const pipe = new FilterExamplePipe();
    expect(pipe).toBeTruthy();
  });
});
